<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="limitless.android.unfollowtita">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:xlargeScreens="true" />

    <application
        android:name=".ApplicationLoader"
        android:allowBackup="true"
        android:icon="@drawable/icon_main"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_main"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:supportsRtl">
        <activity
            android:label="@string/random_user"
            android:name=".Activity.RandomUserActivity" />
        <activity
            android:name=".Activity.DateOfTweetsActivity"
            android:label="@string/t_hour_of_tweets" />
        <activity
            android:name=".Activity.MapOfWordsActivity"
            android:label="@string/t_map_of_words" />
        <activity
            android:name=".Activity.SearchTweetsActivity"
            android:label="@string/search_tweet_for_a_user" />
        <activity
            android:name=".Activity.FirstTweetActivity"
            android:label="@string/text_first_tweet" />
        <activity
            android:name=".Activity.CopyFollowersActivity"
            android:label="@string/copy_followers" />
        <activity
            android:name=".Activity.SearchUsersActivity"
            android:label="@string/search_users" />
        <activity
            android:name=".Activity.TopFriendsActivity"
            android:label="@string/top_friends" />
        <activity
            android:name=".Activity.SettingActivity"
            android:label="@string/menu_setting" />
        <activity
            android:theme="@style/AppTheme"
            android:name=".Activity.UserListActivity" />
        <activity
            android:name=".Activity.LoginActivity"
            android:label="@string/login" />
        <activity
            android:name=".Activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/app_id" />
    </application>

</manifest>